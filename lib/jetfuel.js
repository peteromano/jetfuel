(function(undefined) {
    'use strict';

    module.exports = {
        cli:        require('./cli'),
        util:       require('./util'),
        version:    require('./version')
    };

})(undefined);